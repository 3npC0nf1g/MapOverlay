package com.mapoverlay.model.data.point;


/**
 * Cette classe permet la création d'un segment
 **/
public class InterserctionPoint extends Point{

    /**
     * Crée un nouvel objet InterserctionPoint avec les données spécifiées.
     *
     * @param d Les données du point d'intersection.
     */
    public InterserctionPoint(Point d) {
        super(d);
    }
}
